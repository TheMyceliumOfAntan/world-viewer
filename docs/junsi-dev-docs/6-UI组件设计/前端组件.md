# 前端组件

> 生成时间：2026-09-14 03:58

# 前端组件

## 1. 结构总览

```
src/
  main.tsx        React 入口
  App.tsx         主组件：状态、地图、维度列表、标记、状态栏
  tileLayer.ts    CachedTileLayer：自定义瓦片层
  styles.css      全局样式（深色主题）
```

组件数量少是刻意的：功能集中在 `App.tsx`，唯一需要独立抽象的是瓦片加载逻辑（因为要重写 Leaflet 的 `GridLayer`）。

## 2. `App.tsx`

### 2.1 内部子组件

| 组件 | 职责 |
|------|------|
| `App` | 全部状态与副作用 |
| `WaypointSources` | 侧栏按来源统计当前维度的路径点数量 |

### 2.2 状态

| 状态 | 类型 | 说明 |
|------|------|------|
| `info` | `WorldInfo \| null` | 已加载世界的信息 |
| `error` | `string` | 错误提示 |
| `dim` | `number` | 当前维度 id |
| `ymax` | `number` | 高度切层上限，255 = 全高 |
| `loading` | `boolean` | 加载中标志 |
| `mouse` | `{x,z} \| null` | 鼠标所在的世界坐标 |

### 2.3 引用（ref）

| 引用 | 说明 |
|------|------|
| `mapRef` | Leaflet Map 实例 |
| `mapDivRef` | 地图容器 DOM |
| `layerRef` | `CachedTileLayer` 实例 |
| `markersRef` | 标记图层组 |
| `worldKeyRef` | 当前存档的瓦片 key，参与 URL 生成 |

### 2.4 关键函数

| 函数 | 职责 |
|------|------|
| `ensureMap()` | 惰性创建 Leaflet 地图（`CRS.Simple`，zoom 0..4） |
| `buildTileLayer(map, dim, ymax)` | 创建或复用瓦片层；URL 变化时清缓存 |
| `drawMarkers(map, world, dim)` | 绘制当前维度的路径点与玩家位置 |
| `loadWorld(path)` | 调用 `open_world`，设置状态与视口 |
| `switchDim(id)` | 切换维度并飞到该维度的首个标记或原点 |
| `applyYmax(v)` | 应用高度切层 |
| `pickFolder()` | 打开目录选择对话框 |

### 2.5 坐标系

使用 `L.CRS.Simple`，映射关系：

```
lat = -worldZ
lng =  worldX
```

zoom 0 时 1 瓦片 = 256 世界单位 = 256 方块。

### 2.6 状态栏的 Y 坐标与方块信息

底部状态栏显示 `X= Y= Z=` 与当前列的最高方块（名称 + 原始 id）。Y 不在 `CRS.Simple` 平面内，必须由后端 `probe_block` 查询（见 `3-API规范` §1.5）。

实现要点：

- `mousemove` 注册在 `ensureMap()` 里**只执行一次**，所以处理器通过 `latestRef` 读取当前的 `dim`/`ymax`/是否已加载，避免闭包读到旧值。
- `scheduleProbe(x, z)` 以 150ms 节流：只记录最新坐标，定时器到期才真正发一次 `probe_block`。实测 331 次鼠标移动 → 12 次调用，最小间隔 156ms。
- `runProbe` 用递增的 `probeSeqRef` 序号，仅当回包序号仍是最新时才 `setBlock`，防止慢回包覆盖新结果。
- 未加载存档时不查询，直接清空显示。
- 卸载时清理未触发的定时器。

**状态栏必须保持单行**：`.statusbar span` 设 `white-space: nowrap; overflow: hidden; text-overflow: ellipsis; min-width: 0`。模组方块名可能很长，若允许换行会把状态栏撑成两行并从地图偷走高度（实测地图高度 780→764px）。修复后 400 字符名称下地图高度仍为 780px。

### 2.7 高度滑块跟随世界范围

滑块不再固定 `0..255`，而是绑定当前维度的 `min_y..max_y`（`3-API规范` §2.2）。"全高" 判定改为 `ymax >= range.max`，此时 URL 传哨兵 `4294967295`，由后端解析为该维度自身的 `max_y`。

- **切换维度**要重置高度：各维度范围可能不同（下界 0..255、1.20 主世界 -64..319），沿用旧值会越界，故 `switchDim` 取 `min(ymax, nextRange.max)`。
- 滑块下方显示当前范围（`-64..319`），避免用户对"全高"的含义产生歧义。
- `latestRef` 同时携带 `maxY`，供 `probe_block` 判定是否传哨兵。

### 2.8 渲染开关

侧栏「渲染」区提供三个勾选框，对应 `RenderFlags`（`{ water, shading, altitude }`，默认全开）：

| 勾选框 | URL 参数 | 效果 |
|--------|----------|------|
| 透视水面（显示水底） | `water` | 关则湖泊渲染为单色平面 |
| 地形阴影 | `shade` | 关则不做浮雕着色 |
| 高度明暗 | `alt` | 关则去掉高度项，只留坡度项 |

- 三个开关都**写进瓦片 URL**，因此 `CachedTileLayer` 的模板比较（§3.5）会自动清缓存重绘，无需额外失效逻辑。
- `toggleFlag` 用 `setFlags` 后立即以新值调 `buildTileLayer`，避免依赖 React 状态更新时序。
- `alt` 在 `shading` 关闭时置灰（`disabled`），因为它是 `shade` 的子项。

### 2.9 深链

支持 `?save=<路径>` 查询参数，页面加载后自动打开该存档：

```
http://localhost:1420/?save=C%3A%5C.minecraft%5C...%5C新的世界
```

用途：自动化测试可在刷新页面后恢复世界，也便于手动分享。

## 3. `tileLayer.ts` — `CachedTileLayer`

继承 `L.GridLayer`，替代 `L.tileLayer` 以解决三个问题：

| 问题 | 原生 `L.tileLayer` 的行为 | `CachedTileLayer` 的解法 |
|------|--------------------------|------------------------|
| 平移/缩放时出现空白洞 | 瓦片到达前显示空白 | 缓存 + 父瓦片缩放占位 |
| 请求风暴 | 一次性发出所有可见瓦片请求 | 并发上限 6 + 队列 |
| 重复请求丢失回调 | — | `waiting` 订阅列表，全部通知 |

### 3.1 内部字段

| 字段 | 类型 | 说明 |
|------|------|------|
| `cache` | `Map<string, ImageBitmap>` | 已解码瓦片 |
| `empty` | `Set<string>` | 服务端标记为"无数据"的瓦片 |
| `waiting` | `Map<string, Array<cb>>` | 同一瓦片的待通知回调 |
| `queue` | `Array<() => void>` | 待执行请求 |
| `active` | `number` | 进行中的请求数 |
| `failures` | `Set<string>` | 失败的瓦片，避免无限重试 |
| `urlTemplate` | `string` | 当前 URL 模板 |

### 3.2 加载流程

```
load(key, url, onReady)
  ├─ 命中 cache        → 立即回调
  ├─ 已在 waiting      → 追加回调（关键：不丢弃）
  ├─ 已知失败          → 回调 null
  └─ 否则
       ├─ waiting.set(key, [onReady])
       └─ 入队 → fetch(url, {mode:"cors"})
            ├─ 读 X-Tile-Empty 头
            ├─ createImageBitmap(blob)
            ├─ cache.set(key, bitmap)
            └─ 通知所有 waiting 回调
```

### 3.3 父瓦片占位

`createTile()` 中，若 `z > 0` 且父瓦片（`z-1`，坐标减半）已缓存，则先把它放大 2 倍并裁剪到当前象限绘制到 canvas，同时 `opacity: 1`。清晰瓦片到达后覆盖重绘。

效果：缩放时地图保持连续（模糊），而不是出现空洞。

### 3.4 空数据标记

若响应头 `X-Tile-Empty: 1`，绘制 16px 棋盘格（`rgba(255,255,255,0.035)` / `0.07`），表示"该区域从未生成"。

与"加载中"的区别：加载中是透明（或父瓦片模糊图），无数据是可见棋盘格。

### 3.5 模板切换必须清空队列（ymax / 世界切换）

`setUrlTemplate()` 在模板变化时除了清 `failures`/`empty`，还必须清空 `queue` 与 `waiting`：

```ts
setUrlTemplate(template: string) {
  if (this.urlTemplate === template) return;
  this.urlTemplate = template;
  this.failures.clear();
  this.empty.clear();
  this.queue.length = 0;   // 旧模板的 URL 已失效
  this.waiting.clear();    // 否则回调成为孤儿，同 key 再访问会永久卡住
  this.redraw();
}
```

**为什么**：高度滑块每次 `onChange` 都会重建 URL 模板并 `redraw()`。若保留队列，快速拖动会把旧 ymax 的请求堆到队首，新 ymax 的瓦片排到队尾——实测积压 3133 个请求，地图持续全黑约 35 秒直到积压排空。清空后同一场景 2.5 秒内出图。

`waiting` 必须与 `queue` 同时清：入队前已把回调登记进 `waiting`，只丢队列会让回调永远等不到通知；若用户拖回同一 ymax，该 key 命中 `waiting` 分支被追加而非重新请求，瓦片将永久空白。

### 3.6 小数缩放必须取整（`_clampZoom` 覆写）

地图启用 `zoomSnap: 0.25`（`App.tsx`），滚轮/触控板缩放会停在 `2.5`、`3.5` 这类小数 `mapZoom`。此时点击高度滑块（大跨度切层）会触发 `setUrlTemplate()` → `redraw()`，而 Leaflet 的 `redraw()` 与 `_update()` 直接把 `map.getZoom()` 当瓦片 zoom 用（**不取整**），`_tileZoom` 因此变成 `0.5`，瓦片 key 变成 `-3:-2:0.5`，URL 变成 `/0.5/-3/-2.png`。

后端 `tile_from_uri` 用 `parse::<i32>()` 解析 zoom，小数解析失败 → 协议返回 404 → 前端 `failures` 记录后不再重试 → **地图全黑**。缩放一次会走 `_setView`（其内部 `Math.round`）把 zoom 修正回整数，所以现象是「全黑后随便缩放一次就正常」。

修复：覆写三处路径共用的 `_clampZoom`，在唯一入口取整：

```ts
protected _clampZoom(zoom: number): number {
  const base = L.GridLayer.prototype as unknown as { _clampZoom(zoom: number): number };
  return base._clampZoom.call(this, Math.round(zoom));
}
```

**为什么改这里而不是改 `zoomSnap`**：`_setView`、`redraw`、`_update` 三条路径都经过 `_clampZoom`，一处收口即可，且保留 0.25 平滑缩放粒度。实测：`mapZoom=0.5` + 点击切层，修复前 `dark=100%`、`zs=["0.5"]`（0.5 的 URL `fetch` 直接 `Failed to fetch`）；修复后 `dark=23.1%`、`zs=["1"]`，全部瓦片 200。真实交互（滚轮缩放 + 滑块点击）12/12 通过，其中 6 次命中小数缩放。

### 3.7 诊断接口

`stats()` 返回 `{cached, waiting, queued, active, failed, empty}`，通过 `window.__tileLayer` 暴露，供自动化测试断言。

## 4. 样式

`styles.css` 采用深色主题，主要区块：

| 类名 | 说明 |
|------|------|
| `.app` | 纵向 flex 布局（顶栏 / 主体 / 状态栏） |
| `.topbar` | 标题、存档信息、操作按钮 |
| `.sidebar` | 维度列表、高度滑块、图例、来源统计 |
| `.mapwrap` / `.map` | 地图容器 |
| `.statusbar` | 坐标、维度、层、世界种子、存档名 |
| `.error` / `.loading` | 悬浮提示 |
| `.sources` | 路径点来源统计列表 |
| `.toggles` | 渲染开关勾选框列表 |

## 5. 待办

- 状态栏显示空瓦片统计
- 高度滑块拖动时防抖（当前每次 change 都重建瓦片 URL）

## 6. 相关文档

- `2-架构设计/整体架构.md`
- `3-API规范/Tauri命令与瓦片协议.md`（前端瓦片层契约一节）


## 7. 性能优化
### 1. 颜色解析记忆表（`ColorMemo`，render.rs）

单瓦片渲染中 `Palette::color_ref` 曾是最大热点，占渲染约 60% 时间。根因是
每次调用分配 3 个 `String`：

- `Palette::name_of()` 克隆块名
- `BlockRefRef::to_owned_ref()` 克隆块名
- `tint_kind()` 内 `to_ascii_lowercase()`

而实际去重空间极小：GTNH z=0 瓦片实测 **82944 个地表列，只有 102 种不同
`(id, meta)`**，去重比约 812:1。

`ColorMemo` 按 `BlockRefRef` 记忆 `(rgb, TintKind)`，`biome` 在命中后再套用。
它是**渲染内局部**的，不是共享缓存——共享需要加锁，而每列抢锁会让前端
6 路并发瓦片请求串行化。

实测（同机同存档，对照基线 1dc33cf）：

| 指标 | 优化前 | 优化后 | 提升 |
|------|--------|--------|------|
| 单瓦片 warm | 14.77 ms | 5.21 ms | 2.8x |
| z=0 瓦片 (324 chunks) | 58.18 ms | 27.03 ms | 2.2x |
| viewport 16 瓦片均值 | 12.3 ms/tile | 7.4 ms/tile | 1.66x |
| 并发加速比 | — | 1.68x | 无回归 |
| 并发渲染一致性 | — | 24/24 字节一致 | 无回归 |

一致性由 `tests/memo_regression.rs` 保证：96912 组 `(block, meta, biome)`
组合下 memo 与直接路径结果完全一致；跨缓存历史渲染字节稳定。

### 2. 瓦片请求按视口中心距离排序（`CachedTileLayer.pump`）

Leaflet 按行优先顺序请求瓦片，新视口会把顶部几行先取完，而用户注视的
视口中心反而靠后。队列项改为携带瓦片坐标，`pump` 取距视口中心最近的一项。

只对当前 `_tileZoom` 的项排序：缩放过渡时队列会短暂混合两个 z，跨坐标系
的距离没有意义。线性扫描足够——队列最多一个视口的瓦片数（几十到几百）。

**验收指标是「中心瓦片可见延迟」，不是总耗时**：重排序不减少总工作量，只
改变结果到达顺序，总耗时差异会被噪声淹没。

实测（同机 A/B 各 3 轮，中位数）：

| 指标 | 无排序 | 有排序 | 提升 |
|------|--------|--------|------|
| 中心瓦片可见延迟（中位数） | 202 ms | 120 ms | -41% |
| 中心瓦片可见延迟（均值） | 238 ms | 124 ms | -48% |

### 3. ImageBitmap 生命周期（`closeSoon`）

`CachedTileLayer` 的 LRU 淘汰此前只从 `Map` 删除，从不调用
`ImageBitmap.close()`。位图是 GPU 资源，持续平移会泄漏内存（512 张
256x256 RGBA 约 128 MiB）。

关闭有两个陷阱，`closeSoon` 同时处理：

1. `close()` 是立即生效的，而位图可能仍被 `createTile` 中排队的
   `drawImage`（父瓦片降级绘制与清晰瓦片绘制都在 `setTimeout` 里）引用，
   同轮关闭会抛 `InvalidStateError`。故延后一个宏任务。
2. 延后引入竞态：同一张位图可能在定时器触发前被重新命中（`remember`）。
   故关闭前遍历缓存复核，仍在缓存中的不关。

`clearCache()`（切换世界/高度时调用）同样关闭全部位图。

### 4. 基准脚本

- `scripts/measure-tiles.mjs`：CDP 基准（生产构建，9222 端口）。
- `scripts/measure-tiles-dev.mjs`：dev 模式版，复用已打开页面、端口可传参。
- `scripts/measure-center-latency.mjs`：测中心瓦片可见延迟，即上表指标。


## 修订记录
| 日期 | 版本 | 修改内容 | 修改人 |
| 2026-09-14 | v1.0 | 初版创建 | AI Agent |
| 2026-09-15 | v1.1 | 补充模板切换清空队列契约；状态栏增加世界种子 | AI Agent |
| 2026-09-15 | v1.2 | 补充 _clampZoom 取整契约（小数缩放导致瓦片 404 全黑）；新增状态栏 Y 坐标与方块信息实现 | AI Agent |
| 2026-09-15 | v1.3 | 高度滑块绑定维度动态范围 min_y..max_y，"全高" 哨兵随维度解析 | AI Agent |
| 2026-09-16 | v1.4 | 新增 2.8 渲染开关（水/阴影/高度），原 2.8 深链顺延为 2.9；样式表补 .toggles；移除已完成的生物群系着色待办 | AI Agent |
| 2026-09-17 | v1.5 | 新增性能优化章节：颜色解析记忆表（2.8x）、瓦片请求按视口中心排序（中心延迟 -41%）、ImageBitmap 关闭修复；补实测数据 | AI Agent |

